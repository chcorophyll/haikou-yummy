# 海口美食地图 (Haikou Yummy) 开发计划 🗺️🍜

## 🎯 核心目标与技术栈选型

*   **项目定位**：一款聚焦本地美食打卡、探店推荐的地图应用，兼顾游客的“必吃榜”与本地人的“私藏馆子”。
*   **平台支持**：Web 端 (响应式) + 微信小程序
*   **技术栈 (Tech Stack)**：
    *   **后端 API**：`Python` + `FastAPI` (轻量、高性能，非常适合快速开发和后期数据处理扩展)
    *   **数据库**：`MongoDB` (使用 MongoDB Atlas 免费云服务，文档型数据库极为契合位置数据和非结构化的店铺信息)
    *   **Web 前端**：`React` + `Tailwind CSS` (快速构建现代化、高颜值的界面)
    *   **小程序端**：`UniApp` (基于 Vue，一套代码编译到微信小程序，高效且成熟)
    *   **地图服务**：推荐使用 **高德地图 API** 或 **腾讯地图微信小程序插件**。

---

## 🗺️ 阶段一：MVP 构建 (最小可行性产品) - [已完成 ✅]

**此阶段核心目标：跑通前后端数据链路，验证核心想法。**

### 1. 基础设施搭建
*   **数据库**：[已完成] 注册 MongoDB Atlas 免费集群，创建 `restaurants` 集合，基于 `GeoJSON` 构建空间查询。
*   **测试基建**：[已完成] 修复 `conftest.py` 中可能导致生产数据覆盖的不安全配置，引入安全的隔离测试策略，成功找回丢失数据。
*   **后端 (FastAPI)**：[已完成]
    *   初始化项目结构，连接 MongoDB。
    *   实现核心接口：获取店铺列表、基于地理位置进行附近搜索 (`$near` 查询)、获取单个店铺详情。
*   **版本控制**：[已完成] 连通本地仓库至 GitHub 进行版本管理。

### 2. 数据清洗与精细化补全
*   **动态抓取策略**：[已完成] 编写 `unified_enrichment.py` 统一补全脚本，按优先级智能调用高德地图及腾讯地图 API 抓取详尽数据。
*   **全覆盖质量**：[已完成] 实现了 26 家餐厅**地址、电话、菜系及高质量门店实拍图** 100% 的准确补全，并将无图默认设置落实。
*   **云端与本地同步**：[已完成] 补全后的规范数据已全部同步至 MongoDB Atlas 远端，并在本地 JSON 文件中做妥善备份。

---

## 🛠️ 阶段二：后台管理与内容生态 (V1.5) - [已完成主要特性 ✅]

**此阶段核心目标：丰富地图数据，引入灵活高效的后台可视化管理手段。**

*   **Admin Panel 搭建**：[已完成] 使用 React 开发专属的管理后台，提供清晰的餐厅列表展现与优化后的快速搜索能力。
*   **地图数据智能对接**：[已完成]
    *   打通后台 AMap (高德地图) API，修复高德依赖加载时机和 TypeScript 约束，后台顺利加载外部位置服务。
    *   在编辑或新增餐厅时，自动请求高德接口匹配最新的餐厅地理、联系方式及图片信息反填编辑表单。
*   **防呆验证**：[已完成] 完善强大的防呆检验，譬如编辑器新增餐厅的餐厅名防重验证等，全面对标且超越前端的使用体验。

## 🎨 阶段三：前端 Web 互动应用开发 - [已完成 ✅]

**此阶段核心目标：实现高颜值的双端地图 UI 体系（包括 React Web 与小程序）。**

*   **技术栈**：React + Tailwind CSS + 高德/腾讯地图原生 SDK。
*   **小程序 UI 核心主线任务**：[已完成 ✅]
    *   **暗黑 Tesla 风格 UI**：[已完成] 全面实现 `submit` 页面及地图组件的 Tesla 极简风格，界面 100% 汉化并适配各种屏幕比例（垂直居中修正）。
    *   **零闪烁架构 (Zero-Flicker)**：[已完成] 通过 `pages.json` 原生层级优化与 Vue 原子化渲染锁，彻底消除了详情页切换时的“二次跳变”顽疾。
    *   **交互健壮性**：引入路由防抖机制，防止重复点击导致的渲染队列污染。

*   **位置互通**：支持定位服务联动，以用户自身为中心实现动态附近的店铺展现。
*   **轻量化 UGC**：支持无感知的本地浏览器存储 (LocalStorage 模式) 收藏夹功能。
